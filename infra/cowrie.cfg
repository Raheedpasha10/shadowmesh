[honeypot]
# Hostname the attacker sees when they log in
hostname = bank-srv-01

# Fake OS — looks like a real Ubuntu server
kernel_version = 5.15.0-91-generic
kernel_build_string = #101-Ubuntu SMP Tue Nov 14 13:30:08 UTC 2023

# Use an explicit userdb rule for deterministic post-login validation.
# This keeps the login path simple while we verify bait visibility and
# end-to-end telemetry after a successful SSH session.
auth_class = UserDB

[ssh]
version = SSH-2.0-OpenSSH_8.2p1 Ubuntu-4ubuntu0.11
listen_addr = 0.0.0.0
listen_port = 2222

[output_jsonlog]
enabled = true
logfile = ${honeypot:log_path}/cowrie.json

[output_textlog]
enabled = true
logfile = ${honeypot:log_path}/cowrie.log
